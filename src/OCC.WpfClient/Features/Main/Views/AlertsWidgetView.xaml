<UserControl x:Class="OCC.WpfClient.Features.Main.Views.AlertsWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.Main.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AlertsWidgetViewModel}">
    <Grid MinHeight="150">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Action Center" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                <Border Background="#EF4444" CornerRadius="8" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding AlertCount}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                </Border>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <!-- Warnings List -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Alerts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:SystemAlertItem}">
                            <Button Command="{Binding DataContext.ResolveAlertCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="0,0,0,8" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="cardBorder" Background="#1AE81123" CornerRadius="10" Padding="12,8" BorderThickness="1" BorderBrush="#30E81123">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/> <!-- Icon -->
                                                    <ColumnDefinition Width="*"/>    <!-- Title/Details -->
                                                    <ColumnDefinition Width="Auto"/> <!-- Action Indicator -->
                                                </Grid.ColumnDefinitions>

                                                <!-- Icon -->
                                                <TextBlock Grid.Column="0" Text="{Binding Icon}" Style="{StaticResource SymbolIcon}" Foreground="#EF4444" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>

                                                <!-- Text info -->
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding Message}" Style="{StaticResource FormLabelText}" FontSize="11" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" Opacity="0.8"/>
                                                </StackPanel>

                                                <!-- Action Indicator -->
                                                <TextBlock Grid.Column="2" Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource TextSub}" FontSize="12" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="cardBorder" Property="Background" Value="#33E81123"/>
                                                <Setter TargetName="cardBorder" Property="BorderBrush" Value="#E81123"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
