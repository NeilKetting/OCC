<UserControl x:Class="OCC.WpfClient.Features.Main.Views.ProductivityWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.Main.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProductivityWidgetViewModel}">
    <Grid Height="70">
        <StackPanel VerticalAlignment="Center" Margin="15,0,0,0">
            <TextBlock Text="TASK COMPLETION" Foreground="{StaticResource TextSub}" FontSize="11" FontWeight="Bold"/>
            <TextBlock Text="{Binding CompletionRate, StringFormat={}{0}%}" Foreground="White" FontSize="36" FontWeight="Bold" />
            <TextBlock Text="{Binding CompletionRateText}" Foreground="{StaticResource TextSub}" FontSize="12"/>
        </StackPanel>
        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="36" Height="36" CornerRadius="10" Background="#1A00FF88" Margin="0,0,10,0">
            <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="#00FF88" FontSize="18"/>
        </Border>
    </Grid>
</UserControl>
