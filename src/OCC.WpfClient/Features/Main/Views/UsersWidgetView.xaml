<UserControl x:Class="OCC.WpfClient.Features.Main.Views.UsersWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.Main.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:UsersWidgetViewModel}">
    <Grid Height="70">
        <StackPanel VerticalAlignment="Center" Margin="15,0,0,0">
            <TextBlock Text="USERS" Foreground="{StaticResource TextSub}" FontSize="11" FontWeight="Bold"/>
            <TextBlock Text="{Binding UserCount}" Foreground="White" FontSize="36" FontWeight="Bold" />
            <TextBlock Text="Registered Users" Foreground="{StaticResource TextSub}" FontSize="12"/>
        </StackPanel>
        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="36" Height="36" CornerRadius="10" Background="#1A6B39FF" Margin="0,0,10,0">
            <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="#BE9DFF" FontSize="18"/>
        </Border>
    </Grid>
</UserControl>
