<UserControl x:Class="OCC.WpfClient.Features.Main.Views.BirthdayWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.Main.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:BirthdayWidgetViewModel}">
    <Border CornerRadius="8" Padding="15,10">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#26EC4899" Offset="0"/>
                <GradientStop Color="#13F43F5E" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="#EC4899" FontSize="24" Margin="0,0,15,0" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="Happy Birthday! 🎂🎉" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,2"/>
                <TextBlock Text="{Binding BirthdayGreeting}" Foreground="#FFF3F4F6" FontSize="12" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
