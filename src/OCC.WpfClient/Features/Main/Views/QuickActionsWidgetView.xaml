<UserControl x:Class="OCC.WpfClient.Features.Main.Views.QuickActionsWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.Main.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:QuickActionsWidgetViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid MinHeight="80">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Quick Actions" Foreground="White" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button Content="⚙ Customize" Command="{Binding OpenShortcutPickerCommand}" HorizontalAlignment="Right" Style="{StaticResource ModernTabButton}" Foreground="{StaticResource AccentBlue}" FontWeight="SemiBold"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ActiveQuickActions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" Margin="0,5,0,0"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.NavigateToRouteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                CommandParameter="{Binding Route}" 
                                Margin="0,0,8,8" 
                                Height="32" Padding="12,0" 
                                Style="{StaticResource PrimaryButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding IconCode}" Style="{StaticResource SymbolIcon}" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Label}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Shortcut Customization Modal Overlay -->
        <Grid Grid.RowSpan="2" Background="#A0000000" Panel.ZIndex="1000"
              Visibility="{Binding IsShortcutPickerOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
              HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="-15">
            <Border Style="{StaticResource GlassCard}" 
                    Width="420" Height="420" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Padding="20" CornerRadius="16" BorderThickness="1" BorderBrush="#20FFFFFF">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Customize Quick Actions" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="Select shortcuts to pin to your quick actions." Style="{StaticResource FormLabelText}" FontSize="11" Margin="0,4,0,0" Opacity="0.75"/>
                    </StackPanel>
                    
                    <!-- Checklist -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
                        <ItemsControl ItemsSource="{Binding ShortcutOptions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}" Margin="0,4" Padding="10,0" VerticalContentAlignment="Center">
                                        <CheckBox.Style>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="Foreground" Value="White" />
                                                <Setter Property="FontSize" Value="13" />
                                            </Style>
                                        </CheckBox.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding IconCode}" Style="{StaticResource SymbolIcon}" FontSize="13" Foreground="{StaticResource AccentBlue}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Label}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </CheckBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    
                    <!-- Footer Buttons -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Command="{Binding CancelShortcutsCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Width="80" Height="28"/>
                        <Button Content="Save" Command="{Binding SaveShortcutsCommand}" Style="{StaticResource PrimaryButton}" Width="100" Height="28"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
