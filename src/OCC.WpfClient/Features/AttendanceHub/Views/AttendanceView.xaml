<UserControl x:Class="OCC.WpfClient.Features.AttendanceHub.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.AttendanceHub.ViewModels"
             xmlns:shared="clr-namespace:OCC.WpfClient.Features.AttendanceHub.Views.Shared"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance vm:AttendanceViewModel}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <shared:AttendanceMenuView Grid.Column="0" DataContext="{Binding MenuViewModel}"/>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Background="{StaticResource BackgroundDark}">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</UserControl>
