<UserControl x:Class="OCC.WpfClient.Features.HseqHub.Views.Shared.HealthSafetyMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.HseqHub.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="900">
    
    <Border Style="{StaticResource ModernSidebar}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HSEQ Title Section -->
            <StackPanel Grid.Row="0" Margin="15,20,15,25">
                <TextBlock Text="HSEQ" Foreground="{StaticResource OrangeBrush}" FontSize="24" FontWeight="Black"/>
                <TextBlock Text="MANAGEMENT HUB" Foreground="White" FontSize="10" FontWeight="SemiBold" Opacity="0.5" Margin="2,-2,0,0"/>
            </StackPanel>

            <!-- Navigation Items -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <RadioButton Style="{StaticResource SidebarNavItem}" 
                                 GroupName="HseqTabs"
                                 IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter='Dashboard', Mode=TwoWay}"
                                 Command="{Binding SetActiveTabCommand}" CommandParameter="Dashboard">
                        <StackPanel Orientation="Horizontal" Margin="15,0">
                            <Path Data="{StaticResource IconDashboard}" Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                            <TextBlock Text="Dashboard" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource SidebarNavItem}" 
                                 GroupName="HseqTabs"
                                 IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter='Performance Monitoring', Mode=TwoWay}"
                                 Command="{Binding SetActiveTabCommand}" CommandParameter="Performance Monitoring">
                        <StackPanel Orientation="Horizontal" Margin="15,0">
                            <Path Data="{StaticResource IconLiveView}" Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                            <TextBlock Text="Performance" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource SidebarNavItem}" 
                                 GroupName="HseqTabs"
                                 IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter='Training', Mode=TwoWay}"
                                 Command="{Binding SetActiveTabCommand}" CommandParameter="Training">
                        <StackPanel Orientation="Horizontal" Margin="15,0">
                            <Path Data="{StaticResource IconTeam}" Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                            <TextBlock Text="Training Matrix" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource SidebarNavItem}" 
                                 GroupName="HseqTabs"
                                 IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter='Incidents', Mode=TwoWay}"
                                 Command="{Binding SetActiveTabCommand}" CommandParameter="Incidents">
                        <StackPanel Orientation="Horizontal" Margin="15,0">
                            <Path Data="{StaticResource IconAlert}" Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                            <TextBlock Text="Incidents" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource SidebarNavItem}" 
                                 GroupName="HseqTabs"
                                 IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter='Audits', Mode=TwoWay}"
                                 Command="{Binding SetActiveTabCommand}" CommandParameter="Audits">
                        <StackPanel Orientation="Horizontal" Margin="15,0">
                            <Path Data="{StaticResource IconAudit}" Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                            <TextBlock Text="Audits &amp; Inspections" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource SidebarNavItem}" 
                                 GroupName="HseqTabs"
                                 IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter='Documents', Mode=TwoWay}"
                                 Command="{Binding SetActiveTabCommand}" CommandParameter="Documents">
                        <StackPanel Orientation="Horizontal" Margin="15,0">
                            <Path Data="{StaticResource IconAttach}" Fill="{Binding RelativeSource={RelativeSource AncestorType=RadioButton}, Path=Foreground}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,12,0"/>
                            <TextBlock Text="Documents" VerticalAlignment="Center" FontSize="13"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </ScrollViewer>

            <!-- Bottom Section -->
            <Border Grid.Row="2" BorderBrush="{StaticResource GlassBorderUltraLight}" BorderThickness="0,1,0,0" Padding="15,20">
                <StackPanel>
                    <TextBlock Text="H&amp;S COMPLIANCE" Foreground="White" FontSize="10" Opacity="0.4" HorizontalAlignment="Center"/>
                    <TextBlock Text="SYSTEM ACTIVE" Foreground="{StaticResource PrimaryTeal}" FontSize="9" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
