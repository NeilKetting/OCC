<UserControl x:Class="OCC.WpfClient.Features.HseqHub.Views.TrainingDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.HseqHub.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="550"
             d:DataContext="{d:DesignInstance vm:TrainingDetailViewModel}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Training Record" FontSize="24" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Update employee skills and certifications." FontSize="14" Foreground="{StaticResource TextSub}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,20,0">
            <StackPanel>
                <TextBlock Text="EMPLOYEE" Style="{StaticResource FormLabelText}"/>
                <ComboBox ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" DisplayMemberPath="DisplayName" Style="{StaticResource ModernComboBox}" Margin="0,0,0,20"/>

                <TextBlock Text="SKILL / ROLE" Style="{StaticResource FormLabelText}"/>
                <TextBox Text="{Binding Record.Role}" Style="{StaticResource ModernInput}" Margin="0,0,0,20"/>

                <TextBlock Text="CERTIFICATE TYPE" Style="{StaticResource FormLabelText}"/>
                <ComboBox ItemsSource="{Binding CertificateTypes}" SelectedItem="{Binding Record.CertificateType}" Style="{StaticResource ModernComboBox}" Margin="0,0,0,20">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource FriendlyEnumConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="ISSUE DATE" Style="{StaticResource FormLabelText}"/>
                        <DatePicker SelectedDate="{Binding Record.DateCompleted}" Style="{StaticResource ModernDatePicker}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="EXPIRY DATE" Style="{StaticResource FormLabelText}"/>
                        <DatePicker SelectedDate="{Binding Record.ValidUntil}" Style="{StaticResource ModernDatePicker}"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="TRAINER / PROVIDER" Style="{StaticResource FormLabelText}"/>
                <ComboBox ItemsSource="{Binding Trainers}" Text="{Binding Record.Trainer}" IsEditable="True" Style="{StaticResource ModernComboBox}" Margin="0,0,0,20"/>

                <TextBlock Text="CERTIFICATE DOCUMENT" Style="{StaticResource FormLabelText}"/>
                <Button Command="{Binding UploadCertificateCommand}" Style="{StaticResource ActionButtonSecondary}" Content="Select File" HorizontalAlignment="Stretch" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding CertificateFileName}" Style="{StaticResource LabelText}" Opacity="0.7" TextWrapping="Wrap" HorizontalAlignment="Center"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,20,0,0">
            <Button Style="{StaticResource ActionButtonPrimary}" Command="{Binding SaveTrainingCommand}" Content="SAVE RECORD" Height="45" Margin="0,0,0,10"/>
            <Button Style="{StaticResource CancelButton}" Command="{Binding CloseCommand}" Content="CANCEL" Height="45"/>
        </StackPanel>
    </Grid>
</UserControl>
