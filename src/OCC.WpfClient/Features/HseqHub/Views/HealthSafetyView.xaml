<UserControl x:Class="OCC.WpfClient.Features.HseqHub.Views.HealthSafetyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.HseqHub.ViewModels"
             xmlns:views="clr-namespace:OCC.WpfClient.Features.HseqHub.Views"
             xmlns:shared="clr-namespace:OCC.WpfClient.Features.HseqHub.Views.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <shared:HealthSafetyMenuView Grid.Column="0" DataContext="{Binding MenuViewModel}"/>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Background="{StaticResource BackgroundDark}">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</UserControl>
