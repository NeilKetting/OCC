<UserControl x:Class="OCC.WpfClient.Features.HseqHub.Views.PerformanceMonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.HseqHub.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid Background="{StaticResource BackgroundDark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource ToolbarBorder}" Margin="20,20,20,0">
            <StackPanel Orientation="Horizontal">
                <Path Data="{StaticResource IconLiveView}" Width="24" Height="24" Fill="{StaticResource PrimaryTeal}" Stretch="Uniform" Margin="0,0,15,0"/>
                <TextBlock Text="Performance Monitoring (Safe Hours)" Style="{StaticResource HeaderText}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Data Table -->
        <Border Grid.Row="1" Style="{StaticResource ToolbarBorder}" Margin="20">
            <DataGrid ItemsSource="{Binding SafeHours}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Month" Binding="{Binding Month, StringFormat='MMMM yyyy'}" Width="150"/>
                    <DataGridTextColumn Header="Safe Hours" Binding="{Binding SafeWorkHours}" Width="100"/>
                    <DataGridTextColumn Header="Incidents" Binding="{Binding IncidentReported}" Width="100"/>
                    <DataGridTextColumn Header="Near Misses" Binding="{Binding NearMisses}" Width="100"/>
                    <DataGridTextColumn Header="Root Cause" Binding="{Binding RootCause}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                    <DataGridTextColumn Header="Reported By" Binding="{Binding ReportedBy}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
