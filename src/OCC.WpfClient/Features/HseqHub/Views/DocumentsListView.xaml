<infra:ListViewBase x:Class="OCC.WpfClient.Features.HseqHub.Views.DocumentsListView"
                IsDrawerOpen="{Binding IsOverlayActive}"
                OverlayContent="{Binding ActiveOverlay}"
                DrawerWidth="550"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:infra="clr-namespace:OCC.WpfClient.Infrastructure"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
               xmlns:vm="clr-namespace:OCC.WpfClient.Features.HseqHub.ViewModels"
               xmlns:views="clr-namespace:OCC.WpfClient.Features.HseqHub.Views"
               mc:Ignorable="d" 
               d:DesignHeight="600" d:DesignWidth="1000"
               d:DataContext="{d:DesignInstance vm:DocumentsListViewModel}">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:DocumentDetailViewModel}">
            <views:DocumentDetailView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundDark}">
        <!-- Main Content Wrapper -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource PrimaryTeal}" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Document Control" Style="{StaticResource HeaderText}"/>
                    </StackPanel>
                    <TextBlock Text="Policies, Templates, and Registers" Style="{StaticResource LabelText}" Opacity="0.7"/>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource IconButton}" Command="{Binding OpenUploadCommand}" Margin="0,0,20,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" Style="{StaticResource SymbolButtonIcon}" />
                        <TextBlock Text="Upload Document" Style="{StaticResource ButtonTextStyle}"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Document Grid -->
            <ScrollViewer Grid.Row="1" Padding="24,0,24,24" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Documents}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource GlassCard}" Width="220" Margin="0,0,16,16">
                                <StackPanel>
                                    <Grid Margin="0,0,0,15">
                                        <Border CornerRadius="8" Background="#1A3B82F6" Width="45" Height="45" HorizontalAlignment="Left">
                                            <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource PrimaryTeal}" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <Button HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource IconButtonSmall}" Margin="5"
                                                Command="{Binding DataContext.DeleteDocumentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                            <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource ErrorRed}" FontSize="12"/>
                                        </Button>
                                    </Grid>

                                    <StackPanel Margin="5">
                                        <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis" Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding Category}" FontSize="11" Foreground="{StaticResource TextSub}" Margin="0,0,0,15"/>
                                    </StackPanel>

                                    <Border BorderBrush="{StaticResource GlassBorderLight}" BorderThickness="0,1,0,0">
                                        <Grid Margin="8,5,5,5">
                                            <TextBlock Text="{Binding UploadDate, StringFormat='d MMM yyyy'}" FontSize="10" Foreground="{StaticResource TextSub}" VerticalAlignment="Center"/>
                                            <Button HorizontalAlignment="Right" Style="{StaticResource GhostButton}" Padding="10,4"
                                                    Command="{Binding DataContext.DownloadDocumentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                                <TextBlock Text="DOWNLOAD" FontSize="10" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}"/>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Loading Overlay -->
            <Border Grid.RowSpan="2" Visibility="{Binding IsBusy, Converter={StaticResource VisibilityConverter}}" Background="#80000000">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,16"/>
                    <TextBlock Text="Loading Documents..." Foreground="White"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</infra:ListViewBase>
