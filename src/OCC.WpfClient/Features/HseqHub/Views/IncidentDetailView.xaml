<UserControl x:Class="OCC.WpfClient.Features.HseqHub.Views.IncidentDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.HseqHub.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="550"
             d:DataContext="{d:DesignInstance vm:IncidentDetailViewModel}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource AlertOrange}" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock Text="Incident Report" FontSize="24" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
            <TextBlock Text="Capture details of site health &amp; safety events." FontSize="14" Foreground="{StaticResource TextSub}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,20,0">
            <StackPanel>
                <TextBlock Text="INCIDENT DATE" Style="{StaticResource FormLabelText}"/>
                <DatePicker SelectedDate="{Binding Incident.Date}" Style="{StaticResource ModernDatePicker}" Margin="0,0,0,20"/>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="INCIDENT TYPE" Style="{StaticResource FormLabelText}"/>
                        <ComboBox ItemsSource="{Binding IncidentTypes}" SelectedItem="{Binding Incident.Type}" Style="{StaticResource ModernComboBox}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Converter={StaticResource FriendlyEnumConverter}}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="SEVERITY" Style="{StaticResource FormLabelText}"/>
                        <ComboBox ItemsSource="{Binding IncidentSeverities}" SelectedItem="{Binding Incident.Severity}" Style="{StaticResource ModernComboBox}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Converter={StaticResource FriendlyEnumConverter}}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <TextBlock Text="LOCATION" Style="{StaticResource FormLabelText}"/>
                <TextBox Text="{Binding Incident.Location}" Style="{StaticResource ModernInput}" Margin="0,0,0,20"/>

                <TextBlock Text="DESCRIPTION" Style="{StaticResource FormLabelText}"/>
                <TextBox Text="{Binding Incident.Description}" Style="{StaticResource ModernInput}" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="100" Margin="0,0,0,20"/>

                <TextBlock Text="IMMEDIATE ACTION TAKEN" Style="{StaticResource FormLabelText}"/>
                <TextBox Text="{Binding Incident.CorrectiveAction}" Style="{StaticResource ModernInput}" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="80" Margin="0,0,0,20"/>
                
                <!-- Photos Section -->
                <TextBlock Text="PHOTOS" Style="{StaticResource FormLabelText}" Margin="0,10,0,10"/>
                <Button Command="{Binding UploadPhotosCommand}" Style="{StaticResource ActionButtonSecondary}" Content="+ UPLOAD PHOTOS" Margin="0,0,0,15"/>
                
                <ItemsControl ItemsSource="{Binding Photos}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource GlassCard}" Width="140" Margin="0,0,10,10" Padding="5">
                                <StackPanel>
                                    <Grid Height="80" Margin="0,0,0,5">
                                        <Image Source="{Binding FilePath}" Stretch="UniformToFill"/>
                                        <Button Style="{StaticResource IconButtonSmall}" HorizontalAlignment="Right" VerticalAlignment="Top" 
                                                Command="{Binding DataContext.DeletePhotoCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}">
                                            <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource ErrorRed}" FontSize="10"/>
                                        </Button>
                                    </Grid>
                                    <Button Style="{StaticResource GhostButton}" Command="{Binding DataContext.ViewAttachmentCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding FilePath}">
                                        <TextBlock Text="{Binding FileName}" FontSize="10" TextTrimming="CharacterEllipsis"/>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Documents Section -->
                <TextBlock Text="DOCUMENTS" Style="{StaticResource FormLabelText}" Margin="0,10,0,10"/>
                <Button Command="{Binding UploadDocumentsCommand}" Style="{StaticResource ActionButtonSecondary}" Content="+ UPLOAD DOCUMENTS" Margin="0,0,0,15"/>
                
                <ItemsControl ItemsSource="{Binding Documents}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource GlassCard}" Margin="0,0,0,8" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource PrimaryTeal}" FontSize="14" Margin="0,0,10,0"/>
                                    <Button Grid.Column="1" Style="{StaticResource GhostButton}" HorizontalAlignment="Left" 
                                            Command="{Binding DataContext.ViewAttachmentCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding FilePath}">
                                        <TextBlock Text="{Binding FileName}" Foreground="White" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                    </Button>
                                    <Button Grid.Column="2" Style="{StaticResource IconButtonSmall}" 
                                            Command="{Binding DataContext.DeleteDocumentCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}">
                                        <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource ErrorRed}" FontSize="12"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,20,0,0">
            <Button Style="{StaticResource ActionButtonPrimary}" Command="{Binding SaveIncidentCommand}" Content="SAVE REPORT" Height="45" Margin="0,0,0,10"/>
            <Button Style="{StaticResource CancelButton}" Command="{Binding CloseCommand}" Content="CANCEL" Height="45"/>
        </StackPanel>
    </Grid>
</UserControl>
