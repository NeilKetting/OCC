<UserControl x:Class="OCC.WpfClient.Features.ProjectHub.Views.ProjectDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.ProjectHub.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:ProjectDashboardViewModel}">

    <Grid Margin="0" Background="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Margin="10">
            <Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Text="&#xE9D9;" Style="{StaticResource SymbolIcon}" FontSize="36" Foreground="#107C10" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                    <TextBlock Grid.Column="1" Grid.Row="0" Text="Project Dashboard" FontSize="26" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Grid.Column="1" Grid.Row="1" Text="Monitor construction progress, budgets and timelines across your portfolio." Style="{StaticResource LabelText}"/>
                </Grid>

                <Button HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Command="{Binding CloseCommand}" Style="{StaticResource WindowCloseButton}"/>
            </Grid>
        </Border>

        <!-- Stats Grid -->
        <UniformGrid Grid.Row="1" Columns="5" Margin="10">
            <Border Style="{StaticResource GlassCard}" Margin="0,0,10,0" Padding="15">
                <StackPanel>
                    <TextBlock Text="Active Projects" Foreground="{StaticResource TextSub}" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ActiveProjectCount}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource GlassCard}" Margin="0,0,10,0" Padding="15">
                <StackPanel>
                    <TextBlock Text="Overdue Tasks" Foreground="{StaticResource TextSub}" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding OverdueTaskCount}" Foreground="{StaticResource ErrorRed}" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource GlassCard}" Margin="0,0,10,0" Padding="15">
                <StackPanel>
                    <TextBlock Text="Avg. Completion" Foreground="{StaticResource TextSub}" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding CompletionRate, StringFormat='{}{0:P0}'}" Foreground="{StaticResource SuccessGreen}" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource GlassCard}" Margin="0,0,10,0" Padding="15">
                <StackPanel>
                    <TextBlock Text="Open Incidents" Foreground="{StaticResource TextSub}" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding OpenIncidentsCount}" Foreground="{StaticResource SecondaryGold}" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource GlassCard}" Padding="15">
                <StackPanel>
                    <TextBlock Text="Active Snags" Foreground="{StaticResource TextSub}" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ActiveSnagsCount}" Foreground="#EC4899" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Main Content Area -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation / Quick Actions -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <TextBlock Text="Explore Portfolio" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>

                <Button Command="{Binding GoToRegistryCommand}" Style="{StaticResource ListActionButtonStyle}" Margin="0,0,0,20">
                    <Border BorderBrush="{StaticResource AlertOrange}" BorderThickness="2" CornerRadius="10">
                        <Grid Margin="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="{StaticResource AccentBlue}" FontSize="32" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Projects" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="View and search the complete database of construction projects." Foreground="{StaticResource TextSub}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Button>
            </StackPanel>

            <!-- Activity and Milestones Column -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Recent Activity -->
                <Border Grid.Row="0" Style="{StaticResource GlassCard}" Padding="20" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Recent Updates" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding RecentUpdates}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#10FFFFFF" BorderThickness="0,0,0,1" Padding="0,8,30,8">
                                            <StackPanel>
                                                <TextBlock Text="{Binding TaskName}" FontSize="13" FontWeight="SemiBold" Foreground="White"/>
                                                <TextBlock Text="{Binding ProjectName}" FontSize="11" Style="{StaticResource LabelText}"/>
                                                <Grid Margin="0,4,0,0">
                                                    <TextBlock Text="{Binding DisplayName}" FontSize="10" Foreground="{StaticResource AccentBlue}"/>
                                                    <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:t}'}" FontSize="10" Foreground="#66FFFFFF" HorizontalAlignment="Right"/>
                                                </Grid>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- Upcoming Milestones -->
                <Border Grid.Row="2" Style="{StaticResource GlassCard}" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="Upcoming Milestones" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                            <Border Background="{StaticResource AccentBlue}" CornerRadius="10" Padding="8,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding UpcomingMilestonesCount}" FontSize="10" FontWeight="Bold" Foreground="White"/>
                            </Border>
                        </StackPanel>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding UpcomingMilestones}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#10FFFFFF" BorderThickness="0,0,0,1" Padding="0,10">
                                            <Grid Margin="0,0,30,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Project.Name}" FontSize="12" Foreground="{StaticResource AccentBlue}"/>
                                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding FinishDate, StringFormat='{}{0:ddd, dd MMM}'}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource SuccessGreen}" HorizontalAlignment="Right"/>
                                                    <TextBlock Text="Deadline" FontSize="14" Foreground="#66FFFFFF" HorizontalAlignment="Right"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
