<UserControl x:Class="OCC.WpfClient.Features.ProjectHub.Views.ProjectHseqView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:OCC.WpfClient.Features.ProjectHub.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance vm:ProjectHseqViewModel}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/OCC-ERP;component/Styles/Common.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="SegmentedTabButton" TargetType="RadioButton">
                <Setter Property="Foreground" Value="#88FFFFFF"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Height" Value="28"/>
                <Setter Property="Padding" Value="16,0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6" 
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <ContentPresenter.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="FontSize" Value="11"/>
                                        </Style>
                                    </ContentPresenter.Resources>
                                </ContentPresenter>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                     <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                     <Setter Property="Foreground" Value="White"/>
                                     <Setter TargetName="border" Property="Background" Value="#1500FFFF"/>
                                     <Setter TargetName="border" Property="BorderBrush" Value="#8000FFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundDark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tab Header Bar -->
        <Border Grid.Row="0" Background="#0AFFFFFF" BorderBrush="#15FFFFFF" BorderThickness="0,0,0,1" Padding="20,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Modern Segmented Tab Buttons -->
                <Border Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left"
                        Background="#10FFFFFF" CornerRadius="8" Padding="3" BorderThickness="1" BorderBrush="#3500FFFF">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Style="{StaticResource SegmentedTabButton}" Content="Dashboard" GroupName="ProjectHseqTabs"
                                     IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter=Dashboard, Mode=TwoWay}"
                                     Command="{Binding ShowDashboardCommand}"/>
                        <RadioButton Style="{StaticResource SegmentedTabButton}" Content="Documents" GroupName="ProjectHseqTabs"
                                     IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter=Documents, Mode=TwoWay}"
                                     Command="{Binding ShowDocumentsCommand}"/>
                        <RadioButton Style="{StaticResource SegmentedTabButton}" Content="Incidents" GroupName="ProjectHseqTabs"
                                     IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter=Incidents, Mode=TwoWay}"
                                     Command="{Binding ShowIncidentsCommand}"/>
                        <RadioButton Style="{StaticResource SegmentedTabButton}" Content="Audits" GroupName="ProjectHseqTabs"
                                     IsChecked="{Binding ActiveTab, Converter={StaticResource EqualityConverter}, ConverterParameter=Audits, Mode=TwoWay}"
                                     Command="{Binding ShowAuditsCommand}"/>
                    </StackPanel>
                </Border>

                <!-- Right: Safe Working Hours Stats Badge -->
                <Border Grid.Column="1" VerticalAlignment="Center" Background="#1227E397" BorderBrush="#4027E397" BorderThickness="1" CornerRadius="8" Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <!-- Shield/Safety check symbol -->
                        <TextBlock Text="&#xE73E;" Style="{StaticResource SymbolIcon}" Foreground="#27E397" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="PROJECT SAFE HOURS" Foreground="#80FFFFFF" FontSize="8" FontWeight="Bold"/>
                            <TextBlock Text="{Binding SafeWorkingHours, StringFormat='{}{0:N0} Hours'}" Foreground="White" FontSize="13" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Sub View -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</UserControl>
