<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Clean Dark Theme MenuItem Template -->
    <Style x:Key="ModernMenuItem" TargetType="MenuItem">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Height" Value="45"/>
        <Setter Property="Padding" Value="15,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid x:Name="MenuItemGrid" Background="Transparent" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Hover background -->
                        <Border x:Name="HighlightBorder" Grid.ColumnSpan="3" Background="Transparent" />

                        <!-- Separator line (Tag="HasSeparator") -->
                        <Border x:Name="ItemSeparator" Grid.ColumnSpan="3" BorderBrush="#3b4a54" BorderThickness="0,1,0,0" VerticalAlignment="Top" Visibility="Collapsed" Margin="0,0"/>

                        <!-- Icon -->
                        <ContentPresenter Grid.Column="0" ContentSource="Icon" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0,0,0"/>

                        <!-- Text -->
                        <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center" Margin="10,0,25,0"/>

                        <!-- Submenu Arrow -->
                        <TextBlock x:Name="SubMenuArrow" Grid.Column="2" Text="" FontFamily="pack://application:,,,/Assets/Fonts/#Segoe MDL2 Assets" FontSize="8" Margin="0,0,15,0" VerticalAlignment="Center" Foreground="#99ffffff" Visibility="Collapsed"/>

                        <!-- Popup for Submenu -->
                        <Popup x:Name="PART_Popup" AllowsTransparency="true" Focusable="false" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" Placement="Right">
                            <Border x:Name="SubMenuBorder" Background="#232d36" BorderBrush="#3b4a54" BorderThickness="1" CornerRadius="8" Padding="0,5" Margin="0,0,0,0">
                                <ScrollViewer x:Name="SubMenuScrollViewer" CanContentScroll="True">
                                    <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Cycle" Grid.IsSharedSizeScope="true" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.TabNavigation="Cycle"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="HighlightBorder" Property="Background" Value="#2a3942" />
                        </Trigger>
                        <Trigger Property="Tag" Value="HasSeparator">
                            <Setter TargetName="ItemSeparator" Property="Visibility" Value="Visible" />
                            <Setter Property="Height" Value="46"/>
                        </Trigger>
                        <!-- Submenu Header (has sub-items) -->
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="true">
                            <Setter TargetName="HighlightBorder" Property="Background" Value="#2a3942"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ModernContextMenu" TargetType="ContextMenu">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <ControlTemplate.Resources>
                        <!-- Apply ModernMenuItem style automatically to all MenuItems in this context menu, 
                             avoiding the ItemContainerStyle crash with Separators -->
                        <Style TargetType="MenuItem" BasedOn="{StaticResource ModernMenuItem}"/>
                    </ControlTemplate.Resources>
                    <Border Background="#232d36" CornerRadius="12" Padding="0,5" BorderBrush="#3b4a54" BorderThickness="1">
                        <ItemsPresenter SnapsToDevicePixels="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
