<Window x:Class="OCC.WpfClient.Dialogs.AssignProjectDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ap="clr-namespace:OCC.WpfClient.Infrastructure.AttachedProperties"
        Title="Assign Project" MinHeight="260" Width="550" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    <Border Background="{StaticResource CardBackground}" CornerRadius="10" 
            BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5" />
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="#ffb020" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="Assign to Project / Site" Foreground="White" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- Message -->
            <TextBlock Grid.Row="1" Text="Select the project or custom site location to assign to the selected records:" Foreground="{StaticResource TextSub}" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,12"/>
            
            <!-- Project Selector -->
            <ComboBox x:Name="ProjectCombo" Grid.Row="2" Style="{StaticResource ModernComboBox}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="ProjectCombo_SelectionChanged" Margin="0,0,0,12" Height="40" VerticalContentAlignment="Center"/>
            
            <!-- Custom Site Input -->
            <TextBox x:Name="CustomSiteTxt" Grid.Row="3" Style="{StaticResource ModernInput}" ap:WatermarkProperties.Watermark="Enter custom site location..." Visibility="Collapsed" Margin="0,0,0,12" Height="40" VerticalContentAlignment="Center"/>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="BtnCancel" Content="Cancel" Style="{StaticResource SecondaryButton}" Padding="15,5" Height="35" Margin="0,0,10,0" Click="BtnCancel_Click" BorderBrush="{StaticResource ErrorRed}"/>
                <Button x:Name="BtnOK" Content="Assign" Style="{StaticResource ActionButtonSecondary}" Padding="25,5" Height="35" Click="BtnOK_Click" BorderBrush="{StaticResource SuccessGreen}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
