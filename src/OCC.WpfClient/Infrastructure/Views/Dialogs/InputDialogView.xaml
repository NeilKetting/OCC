<Window x:Class="OCC.WpfClient.Infrastructure.Views.Dialogs.InputDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dialog" MinHeight="260" Width="550" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    <Border Background="{StaticResource CardBackground}" CornerRadius="10" 
            BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5" />
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="" Style="{StaticResource SymbolIcon}" Foreground="#ffb020" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="TitleText" Text="Reason Required" Foreground="White" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            
            <TextBlock x:Name="MessageText" Grid.Row="1" Text="Please enter the reason for adjusting the hours:" Foreground="{StaticResource TextSub}" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,12"/>
            
            <TextBox x:Name="InputTxt" Grid.Row="2" Style="{StaticResource ModernInput}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12" Focusable="True" VerticalContentAlignment="Top"/>
            
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="BtnCancel" Content="Cancel" Style="{StaticResource SecondaryButton}" Padding="15,5" Height="35" Margin="0,0,10,0" Click="BtnCancel_Click" BorderBrush="{StaticResource ErrorRed}"/>
                <Button x:Name="BtnOK" Content="OK" Style="{StaticResource ActionButtonSecondary}" Padding="15,5" Height="35" Click="BtnOK_Click" BorderBrush="{StaticResource SuccessGreen}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
